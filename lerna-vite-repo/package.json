{
  "name": "lerna-vite-repo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "dev": "lerna run dev",
    "test": "lerna run test"
  },
  "devDependencies": {
    "lerna": "^6.0.0",
    "typescript": "^5.0.0"
  }
}