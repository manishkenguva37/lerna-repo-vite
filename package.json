{
  "name": "lerna-vite-monorepo",
  "private": true,
  "workspaces": ["packages/*"],
  "devDependencies": {
    "lerna": "^9.0.0"
  },
  "scripts": {
    "bootstrap": "npm install",
     "dev": "lerna run build --scope artiqui && lerna run dev --parallel",
    "build": "lerna run build"
  }
}
